trigger:
  branches:
    include:
      - main
      - feature/*

pool:
  name: 'mac-local'


stages:

# ============================================================
# STAGE 1: Build & Test
# ============================================================
- stage: Build
  displayName: 'Build and Test'
  jobs:
  - job: RunTests
    displayName: 'Install Dependencies and Test'
    steps:

    - script: |
        /usr/bin/python3 --version
        /usr/bin/python3 -m pip install --user --upgrade pip
        /usr/bin/python3 -m pip install --user -r requirements.txt
        /usr/bin/python3 -m pip install --user pytest
        echo "--- Testing Import ---"
        /usr/bin/python3 -c "from app.main import app; print('App imports OK')"
        echo "--- Cross-compiling for x86_64 Azure deployment ---"
        /usr/bin/python3 -m venv antenv
        antenv/bin/pip install --upgrade pip
        antenv/bin/pip install \
          --platform manylinux2014_x86_64 \
          --target antenv/lib/python3.10/site-packages \
          --only-binary=:all: \
          --python-version 3.10 \
          -r requirements.txt
      displayName: 'Install and Test'
      env:
        OPENWEATHER_API_KEY: 'test-dummy-key'
        OPENWEATHER_BASE_URL: 'http://test-dummy-url'
        APP_ENV: 'dev'

    - script: |
        cd $(System.DefaultWorkingDirectory)
        zip -r $(Build.ArtifactStagingDirectory)/app.zip . \
          -x "*.git*" \
          -x "__pycache__/*" \
          -x "*.pyc" \
          -x "*.db" \
          -x "*.log"
        echo "--- Zip contents (top level) ---"
        unzip -l $(Build.ArtifactStagingDirectory)/app.zip | head -30
      displayName: 'Package app'

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'drop'
      displayName: 'Publish artifact'

# ============================================================
# STAGE 2: Deploy to DEV
# ============================================================
- stage: DeployDev
  displayName: 'Deploy to DEV'
  dependsOn: Build
  condition: succeeded()        # runs on feature/* AND main
  jobs:
  - deployment: DeployToDev
    environment: 'weather-api-dev'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadBuildArtifacts@1
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'drop'
              downloadPath: '$(System.ArtifactsDirectory)'

          - task: AzureWebApp@1
            inputs:
              azureSubscription: 'Azure-Service-Connection'
              appType: 'webAppLinux'
              appName: 'weather-api-dev-jm1535'
              package: '$(System.ArtifactsDirectory)/drop/app.zip'
              startUpCommand: 'gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:8000'

          - script: |
              sleep 60
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://weather-api-dev-jm1535.azurewebsites.net/health)
              if [ "$STATUS" != "200" ]; then
                echo "##[error]Health check failed: $STATUS"
                exit 1
              fi
              echo "Dev health check passed"
            displayName: 'Health check'

# ── Stage 3: Deploy to UAT ────────────────────────────────────────────────
- stage: DeployUAT
  displayName: 'Deploy to UAT'
  dependsOn: DeployDev
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  jobs:
  - deployment: DeployToUAT
    environment: 'weather-api-uat'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadBuildArtifacts@1
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'drop'
              downloadPath: '$(System.ArtifactsDirectory)'

          - task: AzureWebApp@1
            inputs:
              azureSubscription: 'Azure-Service-Connection'
              appType: 'webAppLinux'
              appName: 'weather-api-uat-jm1535'
              package: '$(System.ArtifactsDirectory)/drop/app.zip'
              startUpCommand: 'gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:8000'

          - script: |
              echo "Waiting for UAT to start..."
              for i in {1..10}; do
                STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://weather-api-uat-jm1535.azurewebsites.net/health)
                echo "Attempt $i: Status $STATUS"
                if [ "$STATUS" = "200" ]; then
                  echo "UAT health check passed"
                  exit 0
                fi
                sleep 30
              done
              echo "##[error]UAT health check failed after 10 attempts"
              exit 1

# ── Stage 4: Deploy to PROD ───────────────────────────────────────────────
- stage: DeployProd
  displayName: 'Deploy to PROD'
  dependsOn: DeployUAT
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  jobs:
  - deployment: DeployToProd
    environment: 'weather-api-prod'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadBuildArtifacts@1
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'drop'
              downloadPath: '$(System.ArtifactsDirectory)'

          - task: AzureWebApp@1
            inputs:
              azureSubscription: 'Azure-Service-Connection'
              appType: 'webAppLinux'
              appName: 'weather-api-jm1535'
              package: '$(System.ArtifactsDirectory)/drop/app.zip'
              startUpCommand: 'gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:8000'

          - script: |
              sleep 60
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://weather-api-jm1535.azurewebsites.net/health)
              if [ "$STATUS" != "200" ]; then
                echo "##[error]Health check failed: $STATUS"
                exit 1
              fi
              echo "Prod health check passed"
            displayName: 'Health check'